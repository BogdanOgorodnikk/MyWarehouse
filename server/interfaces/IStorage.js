const IProduct = require('./IProduct')
const IOrder = require('./IOrder')
const IUser = require('./IUser')
const IWarehouse = require('./IWarehouse')

class IStorage {
    async login(email, password) {
    }
}

module.exports = IStorage